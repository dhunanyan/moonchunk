chunk "Main" {
  output: "./dist";
  title: "26 - Final Mandatory Topics";

  let a: int = 1;
  let promoted: float = 1;
  let casted: int = (int)2.8;

  {
    let a: int = 5;
    let localValue: int = a;
    let parentValue: int = parent::a;
    print("localValue:", localValue);
    print("parentValue:", parentValue);
  };

  page "" {
    content {
      <h1>{title}</h1>
      <p>promoted float from int: {promoted}</p>
      <p>casted int from double: {casted}</p>
    };
  };
};

moon(Main);
